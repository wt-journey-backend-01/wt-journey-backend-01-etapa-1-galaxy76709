 Na primiera atividade proposta pelo curso "Webtech Jorney - backend".

____Proposto:____

1: Fornecer arquivos estáticos com Node.js e Express:
    Foi criado uma pasta public para implementacao de um style.css, com o intuito de deixar mais apresentavel.

 2. Receber os dados do formulario, processar e mostrar uma resposta
    Quando o usuario realizar o "post" de seu formulario na pagina inicial, o server receberar seus dados e 
mostrar a pagina de agradecimento, com os dados fornecidos.

3. Mostrar páginas HTML usando modelos (templates)
    Usando arquvis ".ejs", paginas estas que usam os dados para completar o servidor proposto pelo user.
Podendo assim ser mais personalizadas, mostrando agradecimentos, mensagens de conta recebida e erro (404).

5. Criar uma rota que devolve um JSON (simulando uma API)
    Criamos a rota /api/lanches, que não mostra uma página, mas sim uma lista de lanches em formato de dados (JSON).
Esta rota poderia ser usada por um aplicativo ou site que queira mostrar os lanches dinamicamente. (ficsional)